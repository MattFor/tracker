# Tracker

Simple tracker that tracks the projects in your directories, their versions and allows you to track their status/completion progress.

# Roadmap

argument settings to view settings
settings edit to launch the default editor

after everything make it like a standalone executable , upgrade the toml so it's not a copy paste from logeye lol
make a release and mention the codeberg mirror

future:
make it so it can also show actual versions of projects inferred by the preffered language config file of the project f.e cargo.lock, pyproject.toml, package.json